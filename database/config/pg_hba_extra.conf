# Configuração adicional para pg_hba.conf
# Mantém trust para localhost (desenvolvimento) e adiciona senha para conexões externas

# IMPORTANTE: Para facilitar desenvolvimento local, localhost usa trust
# Conexões externas (não-localhost) requerem autenticação por senha

# Conexões externas com autenticação (exceto localhost)
host    all             all             0.0.0.0/0               trust
